# ── Configuration
python-dotenv>=1.0.0
aiofiles>=23.0.0        # requis pour StaticFiles FastAPI

# ── API ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── Auth & Chiffrement ─────────────────────────────────────────────
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0      # fix compatibilité passlib truncate_error
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0      # Fernet AES-128 — chiffrement données santé (RGPD Art. 9)

# ── Validation ─────────────────────────────────────────────────────
pydantic[email]>=2.0.0

# ── Base de données ────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # driver PostgreSQL
aiosqlite>=0.19.0         # driver SQLite async (dev/tests)
alembic>=1.13.0           # migrations de schéma

# ── Rate limiting ──────────────────────────────────────────────────
redis>=5.0.0              # rate limiting multi-process (optionnel, recommandé prod)

# ── Pipeline nutrition (tourne dans le même conteneur que l'API) ────────────
openpyxl>=3.1.0           # lecture CIQUAL xlsx (build_n2_direct.py)
